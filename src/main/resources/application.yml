server:
  port: 8081
  context-path: /demo

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/test
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true